# El mapa — the plan for a map that says where there is something to do

*2026-09-14. The owner (third reply, `docs/ASKS.md`): "additionally, i want to revamp the map... right
now it is not dynamic.. this needs best practices so i can find the next quest or open quests when
its games like my learning -meridian- path." Issue #160 is the ledger row. This is the consolidated
plan from crew run 8 — Tavo, Rosa, Beto, Don Güero, Mari, a two-pass research sweep — with the returns
verbatim in `docs/meetings/2026-09-14-la-cuadrilla-run-8.md`. Chava's cold play was cut by the limit
twice and is owed. Built after the owner's look.*

## 1 · What the map is today `[CODE]`

`drawTown` (`engine/engine.js`, grep `function drawTown`) paints the tiles of **one** world
(`PL.street`) from the pack's colours, then the pack's labels (`TOWNLBL`), **each gated on
`when(flags)`** over `worldFlags()` — which already carries `answered`, `grade`, `closed`, `up` per
district — then a fixed legend line at 7.5 CSS px, then **one** dot from `MAPDOT` for where you stand.
`openMap` fills `#mapov` and adds the name of the room you are in. **Nothing on it reads quest state.**
The state it needs exists: `done`, `qOpen(i)`, `pendingAt(n)`, `worldPending(id)`, `hasSay(n)`.
Measured: 9.39 CSS px per tile at 390 wide; two `MAPDOT` anchors are shared by several worlds; `pa`,
`li`, `ex` have no anchor; **Calle Dos is not on the plan at all** (Rosa §2.2) — a whole street the
plan cannot show, whatever marks it grows.

## 2 · The recommendation — presence on the plan, never a list

All five advisers converge on one shape and refuse the other two:

- **A · Marks on the plan.** *Chosen.* On every door or anchor whose world has somebody waiting
  (`worldPending`), and on every person standing on a drawn world who has something to say
  (`hasSay`), draw **the same ❗ the street draws** — one mark, one meaning, the mark the player
  already knows. Nothing else: no count, no colour ramp, no age, no label.
- **B · A list under the plan.** *Refused, by construction.* It is the banned object of
  `docs/ARCH-LOG.md` A3 — a to-do list with the town as decoration; by day three the plan is
  decoration and the list is the game (Tavo, Rosa, the sweep's M2: *a count is a list with the names
  removed*).
- **C · Both.** Refused: teaches B, because when a picture and a list say the same thing people read
  the list.

**Two marks the crew cut before anyone builds them:** a "finished" tick (the lot→building change
already carries the past, and a third mark on a 9-px plan is the noise the 2026-09-03 junta refused)
and any mark that changes with age or chapter (`docs/OWNER.md`, the mark means one thing forever).

**The one call left open for the owner — a ring on "next".** The mock rendered in the real game
(journey page, screen 12; `docs/mocks/2026-09-14-la-sobremesa/journey/j-mapafter-…`) rings the next
quest. Tavo would keep a single "next"; Rosa cuts it (*no highlight, not a pulse, not a bigger pin*);
the sweep's M3 says Meridian already orders "next" at district level — one district open at a time —
so presence gives it for free and a ring only earns its place inside a district, where `qOpen` has no
order. **Recommendation: build presence first; decide the ring after he has played the plan with
marks on it for a week.** Both sides are cited in the meeting file (Tavo §PART 2; Rosa §2.5 step 3;
the sweep, M3).

## 3 · The seam — a RULE and two CHOICES

| | What | Whose | Cost `[TRAINING]` |
|---|---|---|---|
| RULE | `drawTown` reads quest state and paints presence marks; a pack that declares nothing is byte-identical | engine | ~two lines for the state, one pass for the marks; red first |
| CHOICE | `TOWNPLAN=[{world,ox,oy},…]` — which street worlds the plan draws and where; no declaration = today's one world | pack (Rosa §2.5 step 1) | three lines of content in Meridian, and Calle Dos appears |
| CHOICE | which of `hasSay`'s three clauses a plan counts (the sweep, M1). In Meridian the `doc` clause never fires; in the town it fires on every person, so a behaviour-identical rule would light every district of El Changarrito for ever. **The town declares which clauses count, or declares no marks** | pack | one declaration |
| content | anchors for `pa`, `li`, `ex`; split the shared anchors or accept a two-line chooser on tap (the sweep, M6: a target is a place, ≥24×24 CSS px) | Meridian's `maps.js` | minutes |

## 4 · The tickets that ride with the build (Rosa, run 8)

Filed in her return, §PART 2 and the four tickets under it: the mark's target size on a 9-px grid;
the close and the way back at every row; the legend at 7.5 px (cut it — the mark is its own legend,
a tap names the place: the sweep, M5); what `mapNote` says. Two label collisions measured by the
sweep (M10): `no` under "NOTARIO ⇧", `pa`/`li` on "CALLE DOS →" — `TOWNLBL.y` is a baseline and
`MAPDOT.y` a row, two meanings of `y` in one file.

## 5 · The guard, red first — and the proxy to refuse (Rosa §2.6)

Not *"`drawTown` called `fillText` with ❗ N times"* — a call count where the noun is a mark on a tile
(`docs/REGRESSION.md` §3's commonest shape). The guard reads the plan's pixels at the anchor of a
world with somebody waiting, before and after, with the clocks frozen; and it is planted by a
world with nobody waiting, which must draw nothing.

## 6 · What is owed

- The owner's look at the two pictures (before/after, screen 12 of the journey page), and his word on
  the ring.
- Chava's cold play of the mocked plan (cut twice by the limit).
- Then the build: TOWNPLAN seam → presence marks → anchors → Rosa's four → the guard, one branch, one
  PR, GAMEV + `sw.js` CACHE + the town's tail bumped together.
