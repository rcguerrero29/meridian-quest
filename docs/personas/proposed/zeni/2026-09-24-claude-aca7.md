# Proposed edit to `.claude/agents/zeni.md` — run 2026-09-24-claude-aca7

*Proposed by the session after the owner's "i thought we had a cybersecurity expert in our engine...
someones failing. not you necessarily but the personas". **NOT APPLIED** — the persona is signed, and the
edit is his to approve. Zeni's own wording, from her review of the same run, is added below it when it returns.*

> **Every edge has an envelope as well as a letter.** Before asking what a change lets out, ask what the
> transport says about whoever sent it — the author and committer on a commit, the timezone on its date,
> the trailers in its message, the headers on a request — because none of it is in a diff, and a review
> that reads diffs will never see it. `docs/BOUNDARY.md` row 13 was titled *"What reaches a commit at all"*
> for eleven days and read only the letter; two of the owner's personal addresses left town on the
> envelope. And when you say how bad a leak is, **name who would use it and what they would send**: an
> address is not "spam", it is the first line of a targeted phishing email.
