RUN ID: 2026-09-24-claude-aca7
Agent: claude
Model: not written here — this session's harness keeps model identifiers out of files it pushes; the session reports it on request
Status: verified
Personas: zeni
Issues: 252, 247
Persona learning: docs/personas/proposed/zeni/2026-09-24-claude-aca7.md

# No personal address leaves town — not in a commit, not in a file

## Requested

> whattt you forget about targetted phishing attacks. its ok i dont think im super hard to get but still i thought we had a cybersecurity expert in our engine... someones failing. not you necessarily but the personas... whats next? the fix?

(2026-09-24. Before it: "can you remind me what personal data of mine you previously published again? my email?")

## Tasks

| task | who | model | tokens | how measured |
|---|---|---|---|---|
| what is exposed, read from git and from every issue, PR and comment | claude | not written here (see Model above) | unknown: the session has no per-task usage counter | not measured |
| the guard, its self-test, the hook, the CI step | claude | not written here (see Model above) | unknown: the session has no per-task usage counter | not measured |
| plants, in a copy outside the repository | claude | not written here (see Model above) | unknown: the session has no per-task usage counter | not measured |
| a sweep of the repository for any other personal data, and the register question | zeni | not written here (see Model above) | unknown: filled in when the report returns | not measured |

## Files

- `test/authors.js` — new. Fails on any commit whose author, committer or message carries an address that is not a no-reply one (GitHub's per-account and bot no-reply, GitHub's own, Claude's; RFC 2606 example domains), and on any such address in a file git could ship. Names a domain, never an address, because CI logs are public. `--hook` mode refuses a `git push` before it runs. `--selftest`: 29 cases, fixtures built at run time so the file holds no address of its own.
- `.claude/settings.json` — new. A `PreToolUse` hook on Bash that runs `test/authors.js --hook`: anything but a push passes; a push that would publish a personal address is refused on the machine, before it is public — which CI cannot do.
- `.github/workflows/ci.yml` — the self-test joins the guards' red cases; a step in `smoke` (required) checks the commits a PR or push brings and every shippable file.
- `docs/BOUNDARY.md`, `docs/POSTMORTEM.md` — the edge row 13 never named, and what it cost.
- `docs/ASKS.md`, `docs/NEXT-SESSION.md` — his words, and the corrected date the exposure began.

## Evidence

```
node test/authors.js --selftest            OK — 29 cases, on fixtures, no git, no network.
node test/authors.js origin/main HEAD      OK — every address is a no-reply one
node test/authors.js 3bec58a~1 527a35a     FAIL — 34 lines, 17 commits, each naming gmail.com and no address (the known history, as a measurement)
node test/leaves.js / --selftest           OK / OK — 35 cases
```

Planted outside the repository:

```
P1 · a commit under a personal address        FAIL — commit …: its author address is at gmail.com …
P2 · a personal co-author trailer              FAIL — commit …: its message carries an address at outlook.com …
P3 · an address in an uncommitted doc          FAIL — docs/zz-plant.md:1 holds an address at proton.me …
P4 · an unreadable base                        FAIL — could not read the commits … a red, not a pass
P5 · the allow-list takes a whole mail domain  self-test FAIL — 4 cases
P6 · the message is not read                   self-test FAIL — 1 case
P7 · the red prints the address                self-test FAIL — "it printed the address"
P8 · the allow-list is not anchored            self-test FAIL — the look-alike case
the hook, on a planted commit, for "git push"  exit 2, "PUSH REFUSED …"; for "git status": exit 0
```

**Not proved in this session:** that Claude Code loads the new hook here. The settings file did not
exist when the session started, and the session was refused permission to edit its own hook to prove
it fired. The command was pipe-tested and the file validates; a new session, or `/hooks` once, loads it.

## Acceptance criteria

**For the owner, by hand. In his words, about what he can see — never about files.** At least one
checkbox, and only he ticks them.

- [ ] On this PR, **smoke** is green, and its step *No personal address in a commit or a file* says OK.
- [ ] Issue #252: every box ticked — the accounts behind the two addresses sign in with a passkey.
- [ ] Next time a Claude session pushes from your laptop, the commit on GitHub shows a no-reply address.

## Persona learning

Proposed for Zeni's file, not applied: `docs/personas/proposed/zeni/2026-09-24-claude-aca7.md`. The persona is signed; the edit is the owner's to approve.
