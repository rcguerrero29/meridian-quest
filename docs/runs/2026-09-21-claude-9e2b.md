RUN ID: 2026-09-21-claude-9e2b
Agent: claude
Model: claude-fable-5-1 (the whole run; the owner's /model switch preceded it)
Status: verified
Personas: none — the review ran as a workflow of unnamed lenses and skeptics, not the crew
Issues: none opened; #216 cited
Persona learning: none

# The security review: what only the owner can flip, and the guards an agent could build

## Requested

> ok ensure we take into account cybersecurity best practices ma frend even with agents- i know yall
> are all knowing but this needs to be tight locked so other random agents cant come into our show
> and ruin it yo!
> also question - if we want to start working together, should we have another area for you and
> lets say gemini or chatgpt or grok possibly may discuss things. i may change it depending on their
> performance
> cool give me a list of the next or pending steps, next.

## Tasks

| task | who | model | tokens | how measured |
|---|---|---|---|---|
| the review — five finder lenses, two skeptics per finding, 42 findings, 22 verified; hit the account's session limit in Verify | claude (workflow: 91 agents, 50 done, 41 killed by the limit) | claude-fable-5-1 | 3,550,077 | the workflow's own subagent count |
| the synthesis and the critic, re-run over the saved findings after the limit reset | claude (workflow: 2 agents) | claude-fable-5-1 | 398,016 | the workflow's own subagent count |
| the whole sitting, end to end — the review above, the council protocol, the token rule, ASSUMED:, the smoke fix, eleven guards planted, docs/SECURITY.md | claude | claude-fable-5-1 | 3,355,326 | session usage delta, get_session before and after: 224,721 in + 3,130,605 out; beside it 408.5M cache-read and 16.6M cache-write, which are not in the number; $513.85. The before-snapshot was taken while the first workflow's second finder was already running, so a small slice of the review sits in the previous run's window |

## Files

- `docs/SECURITY.md` — **new.** The one sentence (main is unprotected), three attackers, eight settings
  only he can flip in order, twelve guards built, the AGENTS.md lines, assume-injection, what it does
  not solve, the order. Written from the synthesis with every one of the critic's eighteen findings
  folded in.
- `test/leaves.js` — SHA-pinned `uses:`, no `${{` inside `run:`, five YAML shapes, `secrets()` over
  the tree, `vendored()` hashes, `completeness()` reading `cp` as written, `pages: write` ⇒
  `environment:`, the order channel's citations real; 35 selftest cases.
- `test/runs.js` — refuses a shallow checkout (A4); `grok` in the trailer list.
- `test/issue.js` — **new.** An issue's owner-authored text and nobody else's; an owner issue edited
  by another account withheld. Seven fixture cases.
- `test/town.smoke.js` — the town's CSP pinned byte for byte (A9).
- `test/vendor.sha256` — **new.** Hashes for `vendor/three.min.js` and `qr.js`.
- `.github/CODEOWNERS` — **new.** `*` first, then why each path.
- `.github/workflows/ci.yml` — SHA pins, `fetch-depth: 0`, every expression under `env:`, the
  selftest step. `pages.yml` — SHA pins, `environment: github-pages`, `id: deployment`,
  `workflow_dispatch` dropped.
- `AGENTS.md` — §1 untrusted text and docs-as-proposals, §1½ the credential, §4 fork PRs, §7 three
  rows; `taken:` not `in-flight:`. `CLAUDE.md` mirrors the overlap.
- `docs/BOUNDARY.md` — rows for CODEOWNERS, `icon-192.png`, `icon-512.png`. `docs/RUNS.md` §5's
  sentence made true. `docs/INDEX.md`, `docs/NEXT-SESSION.md`, `docs/CREW-MODE.md`.

## Evidence

Planted, all in copies or fixtures outside the repository, all red where they should be:

```
A4   depth-1 clone: git log -1 -- docs/OWNER.md → HEAD (6e462ca), trailer present → runs.js would have judged HEAD
A9   a CDN host added to the town's script-src → "the town CSP is not the pinned one"
SEC-6  the fixed cp parser, first honest run on the real tree → icon-192.png, icon-512.png with no boundary row
node test/leaves.js --selftest   OK — 35 cases (five YAML shapes, ${{ in run:, moving tag, token in docs/runs/, hash drift, cp line, no environment:, a phantom citation)
node test/runs.js --selftest     OK — 19 cases
node test/issue.js --selftest    OK — 7 cases
```

The five suites and the public build (19 files — the first draft shipped the hash file and a README
to every player, 21) green after every change. CI on the previous push went red on a race in
`smoke.js` check 21 that had nothing to do with the diff, root-caused (the game speaking between the
click and the read) and fixed to read the right noun.

## Acceptance criteria

- [ ] Do `docs/SECURITY.md` §3 step 1 — the ruleset on `main` — before anything else on the page.
      Then tell me whether the click path was right; I wrote it from GitHub's documentation, not from
      your screen.
- [ ] When you add the `smoke` check, confirm the source was offered as *GitHub Actions* and you
      picked it. If the option was not there, say so — the forgery hole then needs another answer.
- [ ] Read step 5 and choose: **Option A** (the second AI works from a fork, no credential) or
      **Option B** (a machine account). A is my recommendation for the first weeks.
- [ ] Open `.github/CODEOWNERS`. Every path there is one you would want to have opened yourself
      before it merged. If any is wrong, name it.
- [ ] Run `node test/issue.js 216` on your laptop. It should print the issue you filed and nothing
      from anyone else. That is what every agent will see from now on.
- [ ] After step 1 is done, a session should read `GET /repos/rcguerrero29/meridian-quest/rulesets`
      and write the date beside the step. Tell me when, and I will.

## Persona learning

`none`.
