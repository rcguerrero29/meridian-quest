RUN ID: 2026-09-24-claude-f97e
Agent: claude
Model: not written here — this session's harness keeps model identifiers out of files it pushes; the session reports it on request
Status: accepted
Personas: none
Issues: 241
Persona learning: none

# The barbería on Calle Dos reads as a barbershop (#241)

## Requested

> yes, close pr. do the barberia, and open a pr for whats waiting. please and than k you and again yeah i didnt say useless you meanie, i literaly said jules can work on site stuff

(2026-09-24, after he stopped the Jules experiment on the same issue: *"what if we decide not to move forward wit jules, just dont merge the pr?"* — logged in `docs/ASKS.md` on the branch of #245.)

## Tasks

| task | who | model | tokens | how measured |
|---|---|---|---|---|
| read #241, Jules' two rounds, the casa code; decide what the fitting-out was | claude | not written here (see Model above) | unknown: the session has no per-task usage counter | not measured |
| the shopfront tile `◫`, 2D and 3D, registered everywhere; one list of house faces | claude | not written here (see Model above) | unknown: the session has no per-task usage counter | not measured |
| the sweet's-pane guard reads the pane and the camera, not 92% of the wall; four plants | claude | not written here (see Model above) | unknown: the session has no per-task usage counter | not measured |
| renders at 390×844, noon, top/front/3D, before and after | claude | not written here (see Model above) | unknown: the session has no per-task usage counter | not measured |

## Files

- `content/meridian/art.js` — `◫`, EL ESCAPARATE: `TILEART` (front and top cameras) and `TILEART_MESH` (3D), built on the house's own `casaBase`/`casaShell` in the order a barber fits out a front room: the window knocked wider into one pane with the chair and mirror inside, a striped canvas awning over each window, and one pole on a wall bracket carried by the tile whose east neighbour is a door. `CASA_FACE` gains `◫`; `casaShell`'s `isCasa` and `casaMeterHere` read `CASA_FACE` instead of their own copies. `TILEMETA` `◫` `lift:13` (the roofline's height comes from it). `BUILDTPL.barberia` uses `◫`.
- `content/meridian/maps.js` — `◫` in `SOLIDX` and `MAPCOL`.
- `test/smoke.js` — the roofed-door check reads `CASA_FACE`; check (10), the sweet's pane, counts a part only where it stands between the pane and the camera (the pane's measured top edge, the camera's measured pitch), and says so in its message.
- `content/meridian/config.js`, `sw.js` — `mq-v199`. `changarrito/content/config.js` — its engine half only, `ch-v136 · engine mq-v199` (`test/town.smoke.js` ties the two).
- `docs/mocks/2026-09-24-barberia/` — before and after, noon, 390×844, in 3D, top and front; `before-after.png` is the three side by side.
- `docs/3D-LOG.md`, `docs/REGRESSION.md` — the entry and the register row.

## Evidence

Every CI step, on this branch:

```
node docs/templates/build-branded.js --check              rc=0
node test/smoke.js                                        OK — 60 quests, maxXP 880, all invariants hold.
node test/town.smoke.js                                   OK
node test/engine.smoke.js --index index.html              OK
node test/engine.smoke.js --index changarrito/index.html  OK
node test/gauge.js                                        OK
node test/horno.js                                        OK
node test/runs.js                                         OK
node test/leaves.js --selftest                            OK — 35 cases
node test/runs.js --selftest                              OK — 19 cases
node test/issue.js --selftest                             OK — 7 cases
node test/bump.js origin/main                             OK — "mq-v198" → "mq-v199", and the town's own version moved too
node test/leaves.js                                       OK
```

The door, walked: from (22,2) on `ex`, three steps up → `world: "barberia"`, Naye's shop.

The pane, measured off a sugar skull the engine really placed (`st` 2,0, season forced to muertos): centre 0.45, height 0.281 — so 0.31 to 0.59 — and 0.620 out from the tile's middle.

Red first, for the guard that changed, in a copy **outside** this repository (the lab clean afterwards):

```
BASELINE (this branch)                              OK — 60 quests
A · the valance where it grazed the pane (0.80)     FAIL — the cast work on ◫ at 21,0 stands 0.810 out … runs 0.93 of a tile wide
B · a valance hung in front of the pane             FAIL — the cast work on ◫ at 21,0 stands 0.810 out … 0.93 wide
C · the house window's sill pushed past the pane    FAIL — the cast work on ▩ at 4,2 stands 0.646 out … 0.76 wide
D · the OLD guard (92%-of-wall band), this awning   FAIL — the cast work on ◫ at 21,0 stands 0.810 out … 0.93 wide
```

## Acceptance criteria

**For the owner, by hand. In his words, about what he can see — never about files.** At least one
checkbox, and only he ticks them.

- [ ] Stand on Calle Dos outside the barbería in 3D: it looks like a barbershop, not a house — striped awnings over two shop windows, and a barber pole beside the door.
- [ ] The houses beside it look exactly as they did.
- [ ] Walk through its door: you are in Naye's shop.

## Persona learning

none

## Verdict

Accepted by the owner, 2026-09-24. The lesson: fix things so they always pass to the other worlds, or make that a next step: #249.
