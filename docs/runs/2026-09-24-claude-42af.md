RUN ID: 2026-09-24-claude-42af
Agent: claude
Model: not written here — this session's harness keeps model identifiers out of files it pushes; the session reports it on request
Status: verified
Personas: none
Issues: 247
Persona learning: none

# A check that goes red the day `main` stops being protected

## Requested

> cybersecurity should be key always....

> ok and yeah they should always be near the top of the list or if anything to lists and always be telling me how many there are there but honestly everything should be built with cybersecurity best practices and architecture. through out my system, anyone who talks to me or you or our AI or our agents please.

(2026-09-24. The session offered this guard once the lock was on; the lock went on the same night.)

## Tasks

| task | who | model | tokens | how measured |
|---|---|---|---|---|
| the guard, its self-test, and the CI job | claude | not written here (see Model above) | unknown: the session has no per-task usage counter | not measured |
| plants against GitHub's real answer, outside the repository | claude | not written here (see Model above) | unknown: the session has no per-task usage counter | not measured |
| review of what the change lets out | zeni | not written here (see Model above) | 78283 | the harness's count for the review agent |
| fixes from that review, and plants against the self-test itself | claude | not written here (see Model above) | unknown: the session has no per-task usage counter | not measured |

## Files

- `test/protect.js` — reads GitHub's live rules for `main`; red if deletion or force-push is allowed, no pull request is required, no pull-request rule is left that asks for no review (the owner would be locked out — a second ruleset with a bypass, as `docs/SECURITY.md` step 4 plans, stays green), CI is not required, the required check is not the first job in `ci.yml` (its name read from the file, never typed), is not pinned to GitHub Actions, or up-to-date is off; red too when GitHub cannot be read. Its header says what it cannot see: the bypass list, an edit to itself, what the required job runs, any branch but `main`. `--rules <file>` judges a saved answer; `--selftest` runs twenty-four cases offline, six of them the network half against a stub.
- `.github/workflows/ci.yml` — the self-test joins the guards' red cases inside `smoke`.
- `.github/workflows/protect.yml` — new: the live read, on every PR, every push to `main` and once a day; `contents: read` only, the checkout keeps no token; not a required check.
- `.github/CODEOWNERS` — `test/protect.js` named among the guards (the `*` line already covered it).
- `docs/BOUNDARY.md` — row 11 names the new read, what it sends and what it cannot see; the list gains `test/protect.js` and `protect.yml`. Row 11's older lines that said nothing runs `leaves.js --selftest` (`smoke` has since 2026-09-20), the fixture count, and the triggers are corrected; G9 marked half closed (`closes.js --selftest` still runs nowhere).
- `docs/SECURITY.md` — step 1 marked done, and guarded. `docs/NEXT-SESSION.md` — the security block names the guard and the deploy approval.

## Evidence

```
node test/protect.js --selftest        OK — 24 cases, on fixtures, no network.
node test/protect.js --rules live.json OK — main is locked … (GitHub's real answer, saved 2026-09-24)
node test/leaves.js                    OK
node test/leaves.js --selftest         OK — 35 cases
```

Planted outside the repository, against GitHub's real answer changed one rule at a time:

```
A · deletion allowed               FAIL — main can be deleted — the "restrict deletions" rule is gone
B · the check unpinned             FAIL — the "smoke" check main waits for is not pinned to GitHub Actions …
C · up to date off                 FAIL — main no longer requires a branch to be up to date before merging …
D · one approval required          FAIL — main now requires an approving review … this locks him out of every merge
E · no rules at all                FAIL — main has no protection at all …
F · ci.yml's job renamed           FAIL — main waits for "smoke" but not for "tests", the CI job that runs every suite
G · GitHub will not answer         FAIL — could not read main's rules from GitHub (HTTP 401) … a red, not a pass
```

**Correction, from Zeni's review.** G's "HTTP 401" was printed by a draft without the retry. The code as merged would print the SECOND answer (here 403, which is what this container gets) — and the retry itself had run in no test. Both are now self-test cases with GitHub played by a stub.

After the review, the same saved answer again, and edits to the guard itself (in a copy outside the repository):

```
the real answer + the plan's step-4 ruleset (1 approval, code owners)   OK   — a bypassed ruleset is not a lockout
the lock asks for a code owner's review                                  FAIL — every pull-request rule on main now asks for an approving review or a code owner's …
the lock asks for one approval                                           FAIL — the same sentence
P1 · the retry keeps the token          self-test FAIL — 3 of 24
P2 · code-owner review ignored          self-test FAIL — 1 of 24
P3 · any approval anywhere is red       self-test FAIL — 1 of 24 (the step-4 case)
P4 · no retry at all                    self-test FAIL — 3 of 24
P5 · a refused answer is read anyway    self-test FAIL — 3 of 24
node test/protect.js  (this container)  FAIL — … GitHub answered HTTP 403 for the rules of main … a red, not a pass
```

The live read cannot reach GitHub from the session's container (its stand-in token is refused, and without it the proxy answers 403), and it says so in red. **Its live evidence is the `protection` job on this PR's own CI run.**

## Acceptance criteria

**For the owner, by hand. In his words, about what he can see — never about files.** At least one
checkbox, and only he ticks them.

- [ ] On this PR, the checks list shows a green **protection** next to **smoke**.
- [ ] Tomorrow, in the Actions tab, a **Protection** run started by *schedule* is green.
- [ ] In Settings → Rules, `main-lock` is still active with nobody on its bypass list (the one part this check cannot see).

## Persona learning

none
