RUN ID: 2026-09-24-claude-42af
Agent: claude
Model: not written here — this session's harness keeps model identifiers out of files it pushes; the session reports it on request
Status: verified
Personas: none
Issues: 247
Persona learning: none

# A check that goes red the day `main` stops being protected

## Requested

> cybersecurity should be key always....

> ok and yeah they should always be near the top of the list or if anything to lists and always be telling me how many there are there but honestly everything should be built with cybersecurity best practices and architecture. through out my system, anyone who talks to me or you or our AI or our agents please.

(2026-09-24. The session offered this guard once the lock was on; the lock went on the same night.)

## Tasks

| task | who | model | tokens | how measured |
|---|---|---|---|---|
| the guard, its self-test, and the CI job | claude | not written here (see Model above) | unknown: the session has no per-task usage counter | not measured |
| plants against GitHub's real answer, outside the repository | claude | not written here (see Model above) | unknown: the session has no per-task usage counter | not measured |

## Files

- `test/protect.js` — reads GitHub's live rules for `main`; red if deletion or force-push is allowed, no pull request is required, an approval is required (the owner would be locked out), CI is not required, the required check is not the CI job that runs every suite (its name read from `ci.yml`, never typed), is not pinned to GitHub Actions, or up-to-date is off; red too when GitHub cannot be read. `--rules <file>` judges a saved answer; `--selftest` runs fifteen cases offline.
- `.github/workflows/ci.yml` — the self-test joins the guards' red cases inside `smoke`; a new job, `protection`, runs the live read and is not a required check.
- `docs/BOUNDARY.md` — row 11 names the new read and what it sends; the list gains `test/protect.js`.
- `docs/SECURITY.md` — step 1 marked done, and guarded. `docs/NEXT-SESSION.md` — the security block names the guard and the deploy approval.

## Evidence

```
node test/protect.js --selftest        OK — 15 cases, on fixtures, no network.
node test/protect.js --rules live.json OK — main is locked … (GitHub's real answer, saved 2026-09-24)
node test/leaves.js                    OK
node test/leaves.js --selftest         OK — 35 cases
```

Planted outside the repository, against GitHub's real answer changed one rule at a time:

```
A · deletion allowed               FAIL — main can be deleted — the "restrict deletions" rule is gone
B · the check unpinned             FAIL — the "smoke" check main waits for is not pinned to GitHub Actions …
C · up to date off                 FAIL — main no longer requires a branch to be up to date before merging …
D · one approval required          FAIL — main now requires an approving review … this locks him out of every merge
E · no rules at all                FAIL — main has no protection at all …
F · ci.yml's job renamed           FAIL — main waits for "smoke" but not for "tests", the CI job that runs every suite
G · GitHub will not answer         FAIL — could not read main's rules from GitHub (HTTP 401) … a red, not a pass
```

The live read cannot reach GitHub from the session's container (its stand-in token is refused, and without it the proxy answers 403), and it says so in red. **Its live evidence is the `protection` job on this PR's own CI run.**

## Acceptance criteria

**For the owner, by hand. In his words, about what he can see — never about files.** At least one
checkbox, and only he ticks them.

- [ ] On this PR, the checks list shows a green **protection** next to **smoke**.
- [ ] In Settings → Rules, `main-lock` is still active with nobody on its bypass list (the one part this check cannot see).

## Persona learning

none
