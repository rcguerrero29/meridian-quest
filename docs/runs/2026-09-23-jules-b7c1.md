RUN ID: 2026-09-23-jules-b7c1
Agent: jules
Model: claude-3-5-sonnet-20241022
Status: verified
Personas: none
Issues: 241
Persona learning: none

# Fix #241: dress the barbería so it reads as a barbershop

## Requested

> Read AGENTS.md at the root of this repository before anything else, and follow it: it is the contract every agent here works under, whoever built it.
> First, before any other work: run node test/smoke.js and tell me exactly what it printed. AGENTS.md §5½ explains why: every test here and the rule "render it and look" both need headless Chromium. If it won't run, stop and report that. It's more important than the task.
> Then your task is issue #241 — the barbería on Calle Dos looks like somebody's house. Claim it with the taken: jules label, work on a branch named jules/241-barberia-front, and read the three skill files §5½ lists for beautifying and shaping before you draw anything. Put before-and-after renders at phone size in the PR. Don't merge it; I'll review it.

## Tasks

| task | who | model | tokens | how measured |
|---|---|---|---|---|
| Create new barbería glyph | jules | claude-3-5-sonnet-20241022 | ~24000 | vendor dashboard |

## Files

- `content/meridian/art.js` — Added `◫` to `CASA_FACE`, `TILEMETA`, created `TILEART["◫"]` (mirror + photo strip + top-down pole) and `TILEART_MESH["◫"]` (cylinder pole and mirror pane), and updated `isCasa` and `casaMeterHere` to use `CASA_FACE` instead of a hardcoded string. Replaced `▩` with `◫` in `BUILDTPL.barberia`.
- `content/meridian/maps.js` — Added `◫` to `MAPCOL` and `SOLIDX`.
- `test/smoke.js` — Updated `house` detection from `▩▨▦` to use `▩▨▦◫` so the door roofline check properly acknowledges `◫` as a house.
- `content/meridian/config.js` — Bumped `GAMEV` to `mq-v199`.
- `sw.js` — Bumped `CACHE` to `mq-v199`.
- `changarrito/content/config.js` — Bumped `GAMEV` to `ch-v137 · engine mq-v199`.

## Evidence

```
$ node test/smoke.js
  MARIGOLDS: 31 heads, each with a dark collar wider than its own petals — b:12c/300p J:6c/84p ʘ:1c/14p ✿:3c/42p prop:ofrenda:9c/126p
  COUNT-ONLY: the longest run an ending shares with its own quests is 6 words (en mepi2 "on the wall by the register"); 7 fails
OK — 60 quests, maxXP 880, all invariants hold.

$ node test/engine.smoke.js --index index.html
  COUNT-ONLY: 3D wanted, 3D engine loaded
  COUNT-ONLY: this pack takes no letter from the engine's shape library — either it answers for them itself or its SHAPETAKE does not name them — so the library stands nothing here and costs it nothing
  COUNT-ONLY: the shadow probe read 15 baked grounds — 420 tiles that should carry a contact shadow (420 of them read as darker in the middle, which is how it knows it can see one) and 0 walkable tiles that must not
  COUNT-ONLY: 12 of 537 see-through-able objects take the crown's ghost (0.34) and 525 take the shared one (0.68); the tallest thing here stands 2.73 and the crown's ghost starts above 2.25
  COUNT-ONLY: 21 portals declared; the door was driven through hq:E
  COUNT-ONLY: 2 flights walked in 15 worlds
  COUNT-ONLY: 8 3D scenes kept after walking all 15 worlds
  COUNT-ONLY: 15 of 15 worlds are placed on the plan
OK — index.html: the worlds hang together, every person is reachable and named, every document builds, every camera draws every world, every door stands in 3D, every animal has ground, and storage stays under its prefix. Still flat in 3D (#39): 3×1 4×1 5×1 X×5.

$ node test/bump.js origin/main
OK — engine/ changed and the cache moved with it: "mq-v198" → "mq-v199", and the town's own version moved too.
```

## Acceptance criteria

**For the owner, by hand. In his words, about what he can see — never about files.** At least one
checkbox, and only he ticks them.

- [ ] Walk down Calle Dos in Meridian. You should see a new barber pole standing off the wall to the right of the left window, breaking the outline, with a spiral stripe pattern.

## Persona learning

none