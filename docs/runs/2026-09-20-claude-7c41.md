RUN ID: 2026-09-20-claude-7c41
Agent: claude
Model: claude-opus-5, then claude-fable-5-1 (the owner switched models with /model partway through the sitting; the protocol was written on Opus 5, the council and the token rule on Fable 5.1)
Status: accepted
Personas: none — this run wrote a protocol, it did not use the crew
Issues: none
Persona learning: none

# The two-AI protocol: a contract any agent reads, a named run, and an acceptance gate that is the owner's

## Requested

> merge please - i also want to be able to handle another AI to use our engine, how can we make sure
> that you can work together- meaning you'd have to have things labeled as in flight on our backlog
> and ensure to review prs for conflicts and each crew run should have a unique run ID and show:
>
> * Which persona/agent did each task, vs you or the other AI working on it etc
> * The exact request, files or backlog items affected, and test/verification evidence.
> * Status: proposed, implemented, verified, accepted by me, or rejected.
> * Any persona-learning suggestion kept separate from the approved persona itself.
> * verification tests for me to complete that can be tracked automatically via acceptance criteria
>   but can only be closed by me the owner of the product. no offense and i hope one day you can help
>   me more but as you can review product development cycles, the people who are responsible for
>   requirements needs to give the approval
>
> ANY other suggestions?

## Tasks

| task | who | model | tokens | how measured |
|---|---|---|---|---|
| AGENTS.md, RUNS.md, the template, test/runs.js, test/overlap.js, this file | claude | claude-opus-5 | unknown: the token rule was written after this task, and no before-snapshot exists. Session cumulative at the moment the rule was written: 725,595 in · 3,333,113 out · 868M cache-read · $644.48 since 2026-09-15 | unknown: no before-snapshot |
| the council protocol and its guard | claude | claude-fable-5-1 | unknown: same — written before the rule existed | unknown: no before-snapshot |
| the token-and-model rule itself (this table, RUNS.md §3½, the guard) | claude | claude-fable-5-1 | unknown: the before-snapshot IS this task's output, so its own cost lands in the next run's delta | unknown: measurement starts with the next run |

## Files

- `AGENTS.md` — **new.** The cross-vendor contract at the repository root. `agents.md` is a real
  convention, stewarded by the Agentic AI Foundation under the Linux Foundation and read by OpenAI
  Codex, Cursor, Jules, Amp and Factory. One file governs every agent instead of each vendor reading
  its own dialect.
- `docs/RUNS.md` — **new.** The run protocol: the ID, the one-file-per-run rule and why, the five
  statuses, the acceptance gate and its honest limit, the overlap tool, and what this does not solve.
- `docs/runs/TEMPLATE.md` — **new.** The shape every run copies.
- `docs/runs/2026-09-20-claude-7c41.md` — **new.** This file: the format demonstrated, not described.
- `test/runs.js` — **new.** The guard, plus `--selftest` on fixtures outside the repository.
- `test/overlap.js` — **new.** Which other branch is holding a file this one touches. Git alone.
- `.github/workflows/ci.yml` — one step: `node test/runs.js`.
- `docs/CREW-MODE.md` — the claim label is now `in-flight: <agent>` and names *which AI*, not only
  which persona.
- `CLAUDE.md`, `README.md`, `docs/INDEX.md` — point at `AGENTS.md` and list the new documents.
- `docs/ASKS.md` — his words, verbatim.

## Evidence

```
node test/runs.js --selftest
  ok   a well-formed run is green
  ok   the filename disagrees with the RUN ID
  ok   the ID is not the format
  ok   Agent disagrees with the name inside the ID
  ok   a status nobody defined
  ok   acceptance criteria with no checkbox
  ok   the request left as the template placeholder
  ok   persona learning aimed at the approved persona
  ok   a missing section
  ok   no ledger at all
OK — 10 cases, planted on fixtures outside this repository.
```

**`test/overlap.js`, planted against two agents holding the same file** — a throwaway clone outside
this repository, with `origin/codex/900-window` and `origin/claude/901-map` both appending to
`engine/engine.js`:

```
⚠ origin/claude/901-map  ×  origin/codex/900-window  — 1 file(s) in both:
    engine/engine.js
overlap: 3 live branch(es) compared, 1 overlapping pair(s).
```

A branch touching only `docs/OWNER.md` reported `0 overlapping`. **And the shallow case refuses
rather than lying** — `git clone --depth 1`, then:

```
overlap: there is no main to measure against in this checkout, so this said nothing — that is not "no overlap".
```

The five suites, green, plus the new one:

```
node test/smoke.js                                    OK — 60 quests, maxXP 880, all invariants hold.
node test/town.smoke.js                               OK — the town boots on its own prefix …
node test/engine.smoke.js --index index.html          OK — the worlds hang together …
node test/engine.smoke.js --index changarrito/index.html   OK
node test/gauge.js                                    OK — the gauge …
node test/runs.js                                     BOARD: 1 run(s) — 0 proposed · 0 implemented · 1 verified …
```

Working tree clean after every plant; nothing was planted inside this repository.

## Acceptance criteria

- [ ] Read `AGENTS.md` top to bottom. If a second AI followed only that file, would you be
      comfortable letting it near the engine? If any rule there is not one you'd actually enforce,
      say which.
- [ ] Section 8 says `verified` is the furthest any agent may take anything, and `accepted` /
      `rejected` are yours alone. Confirm that is the line you meant.
- [ ] Open `docs/runs/2026-09-20-claude-7c41.md` — this file. It is the format every future run
      takes. Is it the thing you'd want to read a week later to know what happened?
- [ ] Run `node test/runs.js` on your laptop. The `BOARD:` line is the "what is waiting on me" view.
      Tell me if it should say something else.
- [ ] Run `node test/overlap.js --all` on your laptop. With one branch it will say so; the shape is
      what matters.
- [ ] Decide the second AI's short name (`codex`, `gemini`, whatever it is). It becomes its branch
      namespace, its `in-flight:` label and the word inside its run IDs, so it is worth picking once.

**None of these are ticked by me.** That is the whole point of §8.

## Persona learning

`none`. No persona was used or changed by this run.

## Verdict

Accepted by the owner, 2026-09-24. His note: other AIs are not as good or as thorough.
